// components/notification.js — toast/snackbar de notificação (.toast)

export function mostrarToast(mensagem, tipo = 'info') {
  // TODO: injetar .toast temporário no DOM e remover após alguns segundos
}
