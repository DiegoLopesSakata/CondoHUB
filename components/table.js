// components/table.js — tabela reutilizável (.data-table)

export function renderTable({ colunas, linhas }) {
  // TODO: gerar <table class="data-table"> a partir de colunas [{ chave, titulo }] e linhas (array de objetos)
  return '';
}
