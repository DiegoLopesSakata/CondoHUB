// components/form.js — helpers de formulário (.form-field .form-label .form-input ...)

export function campoTexto({ id, label, valor = '', obrigatorio = false }) {
  // TODO: markup de .form-field com <label for> associado ao input (a11y)
  return '';
}

export function validarCampos(form) {
  // TODO: validação genérica de campos obrigatórios
  return true;
}
